# 部署與啟動

## 目的

管理部署、啟動、環境變數、排程、Windows / Linux 啟動與 Hosting。

## 適用情境

當需求涉及部署、啟動、環境變數、排程、Windows 啟動、Linux 啟動或 Hosting 時，AI Agent 應閱讀此文件。

## 目前內容

Windows 本機執行，使用 `npm.cmd install` 與 `npm.cmd start`，也可雙擊 `start-widget.cmd`。

## 規則

回覆、文件、分析與註解預設使用繁體中文；程式碼、API 名稱、套件名稱、類別名稱、函式名稱、檔案名稱、環境變數名稱與 Git 指令保留原文。

## 範例

待補充。

## 常見錯誤

待補充。

## 注意事項

每次查詢、分析或修改後，若產生可重用結論，必須回寫到本文件或其他對應 docs 文件。修改後也必須提醒使用者提交 Git。