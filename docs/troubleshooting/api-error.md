# API 錯誤

## 目的

管理 API、Webhook、HTTP status code、Token 與外部服務錯誤。

## 適用情境

當需求涉及 API 錯誤、Webhook 錯誤、HTTP 錯誤或 Token 問題時，AI Agent 應閱讀此文件。

## 目前內容

待補充。

## 規則

回覆、文件、分析與註解預設使用繁體中文；程式碼、API 名稱、套件名稱、類別名稱、函式名稱、檔案名稱、環境變數名稱與 Git 指令保留原文。

## 範例

待補充。

## 常見錯誤

待補充。

## 注意事項

每次查詢、分析或修改後，若產生可重用結論，必須回寫到本文件或其他對應 docs 文件。修改後也必須提醒使用者提交 Git。