# 測試規範

## 目的

管理本專案可用的測試、驗證方式與測試風險。

## 適用情境

當需求涉及測試、驗證、修正後確認或 CI 檢查時，AI Agent 應閱讀此文件。

## 目前內容

啟動後確認小窗出現在右下角，且能讀到 `%USERPROFILE%\.codex\sessions` 中的 rate limit 資料。

## 規則

回覆、文件、分析與註解預設使用繁體中文；程式碼、API 名稱、套件名稱、類別名稱、函式名稱、檔案名稱、環境變數名稱與 Git 指令保留原文。

## 範例

待補充。

## 常見錯誤

待補充。

## 注意事項

每次查詢、分析或修改後，若產生可重用結論，必須回寫到本文件或其他對應 docs 文件。修改後也必須提醒使用者提交 Git。